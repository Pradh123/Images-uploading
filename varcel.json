{
  "version": 2,
  "builds": [
    { "src": "next.config.js", "use": "@vercel/next" },
    { "src": "server/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/hello/(.*)", "dest": "/server/server.js" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
