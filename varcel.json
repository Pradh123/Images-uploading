{
  "version": 2,
  "builds": [
    { "src": "next.config.js", "use": "@vercel/next" },
    { "src": "serverless/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/hello/(.*)", "dest": "/serverless/server.js" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
